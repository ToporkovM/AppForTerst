# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

pod 'Alamofire'

target 'AppForTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppForTest

  target 'AppForTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppForTestUITests' do
    # Pods for testing
  end

end
